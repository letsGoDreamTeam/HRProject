Commit Title
fix: align booking slot interviewer display and persist interviewer availability

Commit Summary
- 예약 가능 슬롯 응답에 실제 슬롯 배정 면접관 이름(`interviewerNames`)을 포함하도록 수정
- 예약 모달이 직무/차수 기준 전체 면접관이 아니라 슬롯별 실제 면접관만 표시하도록 수정
- 면접관 availability 응답 상태를 메모리 저장소가 아니라 DB 컬럼으로 영속화하도록 수정
- 개발 환경에서 예약 mock fallback이 자동으로 켜지지 않도록 수정
- mock/preview 슬롯 데이터에도 `interviewerNames` 필드를 맞춰 타입 정합성 보완

Changed Files
- back/app/models/interviewer_invite.py
- back/app/repositories/interview_booking_repository.py
- back/app/schemas/interview_booking.py
- back/app/services/interview_booking_service.py
- back/app/services/interviewer_invite_service.py
- back/alembic/versions/d4a1f8c2e9b7_persist_interviewer_availability.py
- front/components/hr/dashboard/ScheduleBookingModal.tsx
- front/components/interview-booking/InterviewBookingClient.tsx
- front/lib/hr/invitation-preview.helpers.ts
- front/types/interviewBooking.ts

Back-End TODO
1. Alembic migration 적용
- 실행 대상: `back/alembic/versions/d4a1f8c2e9b7_persist_interviewer_availability.py`
- 배포 전 DB에 아래 컬럼이 추가되어야 함
  - `interviewer_invites.availability_decision`
  - `interviewer_invites.availability_note`
  - `interviewer_invites.availability_decided_at`

2. 백엔드 마이그레이션 실행 확인
- 예시: Alembic upgrade 실행 후 실제 스키마 반영 여부 확인
- 운영/스테이징 DB 모두 동일하게 반영 필요

3. 예약 가능 슬롯 응답 확인
- `GET /api/interview-bookings/available-slots`
- `GET /api/interview-booking-invitations/{token}/available-slots`
- 응답에 `interviewerNames`가 포함되는지 확인 필요

4. 면접관 availability 저장 확인
- `GET /api/interviewer-invites/{token}/availability`
- `POST /api/interviewer-invites/{token}/availability`
- 서버 재시작 후에도 `decision`, `note`, `decidedAt` 값이 유지되는지 확인 필요

5. 프런트 환경변수 정책 공유
- mock 예약을 쓰려면 `NEXT_PUBLIC_ENABLE_BOOKING_MOCK=true` 를 명시적으로 설정해야 함
- 설정하지 않으면 localhost/dev에서도 실제 오류가 그대로 보이는 것이 정상

Recommended Commit Message
- `fix: align booking slot interviewer display and persist interviewer availability`

Recommended PR Description
- 슬롯별 실제 면접관 정보가 예약 화면에 정확히 보이도록 예약 슬롯 응답을 확장했습니다.
- 면접관 availability 응답 상태를 인메모리 저장소에서 DB 영속 저장으로 변경했습니다.
- 개발용 예약 mock fallback이 실제 오류를 가리지 않도록 명시적 env flag 기반으로 제한했습니다.
